# Corridor ANS recovery row

Development-only recovery of the corridor ANS paired cell reserved during the
next-stage screen. The first reserved directory was started while another ROS
domain-42 run was active and is retained as an overlap anomaly; this row is a
fresh, serial run with the same world/start/condition/seed/budget. It uses the
temporary workspace PyTorch shim described in the sibling recovery study.
